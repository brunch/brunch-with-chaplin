{{#camelize}}{{name}}{{/camelize}} = require 'controllers/{{name}}_controller'

describe '{{#camelize}}{{name}}{{/camelize}}', ->
  beforeEach ->
    @controller = new {{#camelize}}{{name}}{{/camelize}}()
